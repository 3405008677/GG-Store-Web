# 浏览器访问 Nuxt 的同源 API 前缀；不要改成后端的绝对跨域地址。
NUXT_PUBLIC_API_BASE=/api

# 仅供本地开发的 GG.Store.UserManage HTTPS Origin，不包含 /api；修改后需重启 Nuxt。
# 示例：NUXT_API_PROXY_TARGET=https://localhost:7001
NUXT_API_PROXY_TARGET=
